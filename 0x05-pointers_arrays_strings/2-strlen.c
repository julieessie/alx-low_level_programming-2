#include "main.h"

/**
 * _strlen - returns the length of the string
 * @s: the character to be checked
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int l = 0;

	while (*s =! 0)
	{
		l++;
	}

	return (l)
}
