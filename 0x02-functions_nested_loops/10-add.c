#include "main.h"

/**
  * add - Adds two integers
  *@x: the first operand
  *@y: the second operand
  *
  * Return: The sum of the integers
  */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
